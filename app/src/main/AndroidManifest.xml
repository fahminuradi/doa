<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.doahariananak">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DoaHarianAnak">
        <activity android:name=".Kendaraan"></activity>
        <activity android:name=".Musibah" />
        <activity android:name=".Pakaian" />
        <activity android:name=".BeforeBelajar" />
        <activity android:name=".AfterTidur" />
        <activity android:name=".BeforeTidur" />
        <activity android:name=".Hujan" />
        <activity android:name=".OrangTua" />
        <activity android:name=".BangunTidurActivity" />
        <activity android:name=".HomePage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>